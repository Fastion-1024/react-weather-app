import LocationSearch from '../../components/LocationSearch/LocationSearch';

const Home = () => {
    return (
        <main>
            <LocationSearch />
        </main>
    );
};

export default Home;
